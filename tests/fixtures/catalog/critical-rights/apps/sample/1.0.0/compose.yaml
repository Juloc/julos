services:
  app:
    image: example/app:1.0.0
    privileged: true
    network_mode: host
    devices:
      - /dev/ttyUSB0:/dev/ttyUSB0:rwm
    cap_add:
      - NET_ADMIN
    security_opt:
      - seccomp=unconfined
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
