# UX

JulOS uses Fluent 2 Web as its visual foundation while keeping a platform-neutral desktop interaction model.

The immediate desktop release gate and Windows/macOS interaction requirements are defined in `DESKTOP_UX_COMPLETION.md`.
