# Juloc Agent Control bootstrap

Before substantial work in this repository:

1. Read and obey the existing root `AGENTS.md` completely. Its JulOS architecture, security, documentation and validation rules remain mandatory.
2. Read `.agent/project.yaml`.
3. Read the canonical current shared rules from `Juloc/agent-control/AGENTS.md` on `main` using available GitHub access.
4. Inspect relevant JulOS Issues and pull requests plus current coordination in `Juloc/agent-control` issue #1 before claiming work.
5. Create/recheck a coordination claim before substantial edits and release it when the work is complete.

GitHub Issues are the target durable task backlog. Existing work-breakdown/backlog Markdown is a declared legacy transition only until issue #102 completes; new work belongs in GitHub Issues.

If `Juloc/agent-control` cannot be read, continue under the existing JulOS rules, inspect local Issues/PRs for overlap, and record that cross-agent coordination could not be verified.
